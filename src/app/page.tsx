// Renders the server-owned page shell and its interactive application workspace.
import ApplicationBoard from '../components/applications/ApplicationBoard';
export default function Home() {
  return <ApplicationBoard />;
}
